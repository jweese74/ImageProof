# ---------------------------------------------------------------------------
#  Copy this file to `.env` and fill in values for **your** environment.
#  *** NEVER commit the real `.env` (or any other secrets) to version control.
#  If you prefer, you can set the same keys in Apache with `SetEnv` instead.
# ---------------------------------------------------------------------------

DB_HOST=localhost
DB_PORT=3306
DB_NAME=infinite_image_tools
DB_USER=infinite_image_user
DB_PASS=1nfinite_1mage_4assword
DB_DEBUG=false      # true = verbose PDO errors (only use in development)

# Centralised upload-size limit (MB)
MAX_UPLOAD_MB=200
